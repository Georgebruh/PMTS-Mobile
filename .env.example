# Copy to `.env` and fill in real values. `.env` is gitignored.

# Apps Script Web App deployment URL (the /exec endpoint)
EXPO_PUBLIC_PMTS_API_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
